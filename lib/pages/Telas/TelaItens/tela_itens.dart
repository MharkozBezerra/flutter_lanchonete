import 'package:flutter/material.dart';

class TelaItens extends StatelessWidget {
  const TelaItens({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('TelaItens');
  }
}
